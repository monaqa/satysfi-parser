use super::*;

#[test]
fn test_nested() {
    // assert_parsed!(r"\s{}" inline_cmd: [_]);
    // assert_parsed!(r"\s{\s{}}" inline_cmd: [_]);
}

#[test]
fn test_inline_cmd() {
    // assert_parsed!(r"\a{}" inline_cmd: [_]);
    // assert_parsed!(r"\abc-def{}" inline_cmd: [_]);
}
